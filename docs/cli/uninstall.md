---
summary: "CLI reference for `mazelclaw uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `mazelclaw uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
mazelclaw uninstall
mazelclaw uninstall --all --yes
mazelclaw uninstall --dry-run
```
